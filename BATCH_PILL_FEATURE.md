# 批量服用丹药功能实现总结

## 功能概述

为丹药系统添加了批量服用功能，允许玩家一次性服用多个相同丹药。

## 使用方法

```
/服用丹药 <丹药名称> [数量]
```

**示例：**
- `/服用丹药 炼气丹` - 服用1个炼气丹（向后兼容）
- `/服用丹药 炼气丹 10` - 服用10个炼气丹
- `/服用丹药 龙精虎猛丹 5` - 尝试服用5个龙精虎猛丹（受每日限制2个）

## 实现细节

### 1. 修改的文件

#### handlers/pill_handler.py
- **handle_use_pill()** (lines 40-70)
  - 添加数量参数解析逻辑
  - 支持格式：`丹药名称 数量`
  - 验证数量范围：1-999999
  - 错误处理：无效格式、负数、超大数值

#### core/pill_manager.py
- **use_pill()** (lines 98-191)
  - 添加 `quantity: int = 1` 参数
  - 预验证：库存、配置、境界需求
  - 计算最大可服用数量（根据丹药类型限制）
  - 将数量参数传递给各类型处理函数

- **_use_exp_pill()** (lines 193-227)
  - 批量累加经验值
  - 单次数据库更新
  - 显示总经验和单个经验

- **_use_resurrection_pill()** (lines 229-257)
  - 添加 quantity 参数（向后兼容）
  - 实际只处理1个（max_consumable已限制）

- **_use_temporary_pill()** (lines 259-414)
  - 批量延长持续时间
  - 检查是否已有相同效果
  - 已有效果：延长时间
  - 无效果：创建新效果（使用总持续时间）
  - 更新每日使用量（双修丹药）

- **_use_permanent_pill()** (lines 416-590)
  - 循环处理每个丹药
  - 每个丹药检查30%上限
  - 遇到上限停止
  - 累加总增益
  - 显示停止原因

- **_use_instant_pill()** (lines 592-718)
  - 批量累加恢复量
  - 特殊效果只处理一次（重置、护盾）
  - 单次数据库更新

#### main.py
- **命令注册** (line 846)
  - 更新帮助文本：`"服用丹药 [数量]"`

### 2. 限制机制

#### 库存限制
- 自动调整为可用库存数量
- 示例：请求10个，库存5个 → 服用5个

#### 回生丹限制
- 同时只能有1个效果
- 已有效果时拒绝服用
- max_consumable = 1

#### 双修丹药限制（龙精虎猛丹）
- 每日限制：2个/天
- 计算剩余可用量：2 - 已用量
- max_consumable = min(请求量, 剩余量)

#### 永久属性丹药限制
- 终身限制：2个/类型
- 30%属性上限（每个境界）
- 循环处理，遇到上限停止
- 显示实际服用数量和停止原因

### 3. 响应消息格式

#### 修为丹（批量）
```
✨ 成功服用 10 个【炼气丹】！
━━━━━━━━━━━━━━━
📈 获得修为：10000 (1000 × 10)
💫 当前修为：50000
💼 剩余库存：5 个
━━━━━━━━━━━━━━━
```

#### 临时效果丹药（批量）
```
✨ 成功服用 3 个【修炼加速丹】！
━━━━━━━━━━━━━━━
⏱️ 持续时间：180分钟 (60 × 3)
🎯 效果：修炼速度+50%
💼 剩余库存：7 个
━━━━━━━━━━━━━━━
```

#### 永久属性丹药（部分服用）
```
✨ 成功服用 2 个【力量丹】！
⚠️ 已达终身服用上限(2颗)
━━━━━━━━━━━━━━━
💪 永久增益：
  物伤 +200
💼 剩余库存：3 个
━━━━━━━━━━━━━━━
注：每个境界的永久属性丹药
增益最多为基础属性的30%
```

#### 瞬间效果丹药（批量）
```
✨ 成功服用 5 个【回灵丹】！
━━━━━━━━━━━━━━━
🌟 恢复灵气：+2500 (500 × 5)
💫 当前灵气：5000/10000
💼 剩余库存：10 个
━━━━━━━━━━━━━━━
```

### 4. 边界情况处理

| 情况 | 处理方式 |
|------|---------|
| 数量 ≤ 0 | 返回错误提示 |
| 数量 > 999999 | 返回错误提示 |
| 数量 > 库存 | 自动调整为库存数量 |
| 数量 > 每日限制 | 服用至限制上限 |
| 数量 > 终身限制 | 服用至限制上限 |
| 永久丹药达到30%上限 | 停止服用，显示原因 |
| 回生丹已有效果 | 拒绝服用 |
| 无效数量格式 | 返回错误和使用示例 |

### 5. 向后兼容性

- 数量参数完全可选，默认值为1
- 现有命令 `/服用丹药 炼气丹` 继续正常工作
- 所有现有丹药逻辑保持不变
- 无需数据库迁移

## 测试建议

### 基础功能测试
1. `/服用丹药 炼气丹 10` - 基础批量服用
2. `/服用丹药 炼气丹` - 向后兼容（默认1个）
3. `/服用丹药 炼气丹 abc` - 无效格式
4. `/服用丹药 炼气丹 -5` - 负数
5. `/服用丹药 炼气丹 0` - 零

### 限制测试
6. `/服用丹药 炼气丹 100` - 库存不足（假设只有5个）
7. `/服用丹药 龙精虎猛丹 5` - 每日限制（应停在2个）
8. `/服用丹药 [永久丹药] 5` - 终身限制（应停在2个）
9. `/服用丹药 回生丹 10` - 特殊限制（只服用1个）

### 效果测试
10. 批量服用修为丹，验证经验值累加
11. 批量服用临时丹药，验证持续时间延长
12. 批量服用永久丹药，验证30%上限检查
13. 批量服用瞬间丹药，验证恢复量累加

## 性能优化

- 批量操作只进行一次数据库更新
- 预验证减少不必要的处理
- 永久丹药循环中提前终止（遇到上限）

## 代码质量

- ✅ 语法检查通过
- ✅ 所有函数签名已更新
- ✅ 向后兼容性保持
- ✅ 错误处理完善
- ✅ 用户提示清晰
